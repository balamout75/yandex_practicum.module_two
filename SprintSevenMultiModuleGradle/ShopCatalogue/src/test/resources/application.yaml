spring:
  application:
    name: module_two_sprint_five_gradle
  r2dbc:
    pool:
      enabled: false
  liquibase:
    enabled: false
  sql:
    init:
      mode: always
payment_service_url: ${PAYMENT_SERVICE_URL:http://localhost:8081}
tests:
  payment:
    enabled: false
images.path: images/
